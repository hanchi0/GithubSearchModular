import XCTest
@testable import GithubSearchDetail

final class GithubSearchDetailTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GithubSearchDetail().text, "Hello, World!")
    }
}
